{% extends 'base.html.twig' %}

{% block title %}ContactRequest{% endblock %}

{% block body %}
    <h1>ContactRequest</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contact_request.id }}</td>
            </tr>
            <tr>
                <th>DateOfRequest</th>
                <td>{{ contact_request.dateOfRequest ? contact_request.dateOfRequest|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('contact_request_index') }}">back to list</a>

    <a href="{{ path('contact_request_edit', {'id': contact_request.id}) }}">edit</a>

    {{ include('contact_request/_delete_form.html.twig') }}
{% endblock %}
