{% extends 'base.html.twig' %}

{% block title %}Announcer{% endblock %}

{% block body %}
    <h1>Announcer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ announcer.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ announcer.email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ announcer.password }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ announcer.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ announcer.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('announcer_index') }}">back to list</a>

    <a href="{{ path('announcer_edit', {'id': announcer.id}) }}">edit</a>

    {{ include('announcer/_delete_form.html.twig') }}
{% endblock %}
